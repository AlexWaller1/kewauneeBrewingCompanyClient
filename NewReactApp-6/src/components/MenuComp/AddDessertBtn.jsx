import React from 'react'

 const AddDessertBtn = ({ addToCart3 }) => {
  return (
    <button className='btn btn-primary' onClick={addToCart3}>Add To Cart</button>
  )
}

export default AddDessertBtn;
